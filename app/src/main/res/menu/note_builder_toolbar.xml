<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/save_note"
            android:title="Save"
            android:icon="@drawable/ic_action_save"
            app:showAsAction="always"/>

        <item
            android:id="@+id/add_node_dialog"
            android:title="Add node"
            android:icon="@drawable/baseline_add_24"
            app:showAsAction="always"/>

        <item
            android:id="@+id/set_line_connection"
            android:title="Add line"
            android:icon="@drawable/outline_account_tree_24"
            app:showAsAction="always"/>

        <item
            android:id="@+id/delete_node"
            android:title="Delete node"
            android:icon="@drawable/ic_action_bin"
            app:showAsAction="always"/>

    </group>
</menu>

